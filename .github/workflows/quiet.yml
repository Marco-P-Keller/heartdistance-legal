name: Quiet

# Builds the iOS app and runs its tests on a real macOS machine with a real
# Xcode, because a project that has only ever been read is not a project that
# works. Only runs when something under the app actually changes.
on:
  push:
    paths:
      - 'breakthrough/Quiet/**'
      - '.github/workflows/quiet.yml'
  pull_request:
    paths:
      - 'breakthrough/Quiet/**'
      - '.github/workflows/quiet.yml'
  workflow_dispatch:

concurrency:
  group: quiet-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Build and test
    runs-on: macos-15
    timeout-minutes: 30
    defaults:
      run:
        working-directory: breakthrough/Quiet

    steps:
      - uses: actions/checkout@v4

      # The image's own Xcode, not the newest one installed.
      #
      # Picking the newest looked tidier and cost forty minutes: it selected an
      # Xcode whose simulator runtime never finishes booting on this image, and
      # from outside a single xcodebuild call that is indistinguishable from a
      # hung test. The version that ships with the runner is the one whose
      # simulators are known to work. Only reach past it if it is too old for
      # the project format.
      - name: Choose Xcode
        run: |
          set -euo pipefail
          xcodebuild -version
          major=$(xcodebuild -version | sed -n 's/^Xcode \([0-9][0-9]*\).*/\1/p' | head -1)
          if [ "${major:-0}" -lt 16 ]; then
            latest=$(ls -d /Applications/Xcode*.app | sort -V | tail -1)
            echo "Default Xcode $major is older than 16; switching to $latest"
            sudo xcode-select -s "$latest/Contents/Developer"
            xcodebuild -version
          fi
          xcrun simctl list runtimes

      - name: Boot a simulator
        id: sim
        timeout-minutes: 8
        run: |
          set -euo pipefail
          device=$(xcrun simctl list devices available \
            | sed -n 's/^ *\(iPhone [^(]*\) (.*/\1/p' \
            | sed 's/ *$//' \
            | head -1)
          if [ -z "$device" ]; then
            echo "No iPhone simulator on this runner" >&2
            xcrun simctl list devices available >&2
            exit 1
          fi
          echo "Booting $device"
          xcrun simctl boot "$device" || true
          time xcrun simctl bootstatus "$device" -b
          echo "device=$device" >> "$GITHUB_OUTPUT"

      # Split from the run below so that a slow compile and a stuck test are
      # two different lines in the log rather than one silence.
      - name: Build for testing
        run: |
          set -o pipefail
          time xcodebuild build-for-testing \
            -project Quiet.xcodeproj \
            -scheme Quiet \
            -destination "platform=iOS Simulator,name=${{ steps.sim.outputs.device }}" \
            CODE_SIGNING_ALLOWED=NO \
            2>&1 \
            | tee build.log \
            | sed "s|$GITHUB_WORKSPACE/||g" \
            | grep -E "error:|warning:|\*\* BUILD"

      - name: Test
        timeout-minutes: 10
        run: |
          set -o pipefail
          xcodebuild test-without-building \
            -project Quiet.xcodeproj \
            -scheme Quiet \
            -destination "platform=iOS Simulator,name=${{ steps.sim.outputs.device }}" \
            -resultBundlePath TestResults.xcresult \
            -test-timeouts-enabled YES \
            -maximum-test-execution-time-allowance 60 \
            -parallel-testing-enabled NO \
            CODE_SIGNING_ALLOWED=NO \
            2>&1 \
            | tee -a build.log \
            | sed "s|$GITHUB_WORKSPACE/||g" \
            | grep -E "error:|Test Case|Executed |Test Suite .* (passed|failed)|\*\* TEST"

      # The full log is thousands of lines of module compilation. What anybody
      # actually needs after a red run is the handful of lines naming what
      # broke, in one place, without scrolling.
      - name: Digest
        if: always()
        run: |
          {
            echo "## Quiet"
            echo
            echo "### Errors"
            grep -E "error:" build.log | sed "s|$GITHUB_WORKSPACE/||g" | sort -u | head -40 || echo "None."
            echo
            echo "### Failed tests"
            grep -E "^Test Case .* failed" build.log | sort -u | head -40 || echo "None."
            echo
            echo "### Warnings"
            grep -E "warning:" build.log | sed "s|$GITHUB_WORKSPACE/||g" | sort -u | head -60 || echo "None."
            echo
            grep -E "^Executed [0-9]+ test" build.log | tail -2 || true
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Keep the log and the result bundle
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: TestResults
          path: |
            breakthrough/Quiet/build.log
            breakthrough/Quiet/TestResults.xcresult
          retention-days: 7
